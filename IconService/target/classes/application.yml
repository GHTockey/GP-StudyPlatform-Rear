#端口号
server:
  port: 9003

spring:
  application:
    name: icon-service          #服务名
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/gp_icon?useUnicode=true&characterEncoding=utf8
    username: root
    password: 114514
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848   #nacos服务地址
# 输出SQL语句
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  #输出日志
#    map-underscore-to-camel-case: false  #驼峰命名 默认为true